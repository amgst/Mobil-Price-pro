# Vercel Environment Variables
# Copy and paste these into your Vercel dashboard under Settings > Environment Variables
# Make sure to replace placeholder values with your actual credentials

# === REQUIRED VARIABLES ===

# Database Configuration (REQUIRED)
# Replace with your actual Neon database URL
DATABASE_URL=postgresql://neondb_owner:npg_yxlY28rJcMFv@ep-lively-unit-adtogwhk-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Environment (REQUIRED)
NODE_ENV=production

# Site Configuration (REQUIRED)
# Replace with your actual Vercel domain after deployment
VITE_SITE_URL=https://your-app-name.vercel.app

# Admin Authentication (REQUIRED)
# Set secure credentials for admin access
JWT_SECRET=your-super-secure-jwt-secret-key-here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-admin-password-here

# === OPTIONAL VARIABLES ===

# OpenAI Configuration (OPTIONAL)
# Only needed if you want AI analysis features
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# RapidAPI Configuration (OPTIONAL)
# Only needed if you want data import features
# Get your API key from https://rapidapi.com/
RAPIDAPI_KEY=your_rapidapi_key_here

# Port Configuration (OPTIONAL)
# Vercel handles this automatically, but you can set it if needed
PORT=5000

# === INSTRUCTIONS ===
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add each variable above (name and value)
# 4. Make sure to replace all placeholder values with actual credentials
# 5. Deploy your project

# === SECURITY NOTES ===
# - Never commit actual API keys or passwords to version control
# - Use strong, unique passwords for admin access
# - Keep your JWT secret long and random (at least 32 characters)
# - Your database URL is already configured and working